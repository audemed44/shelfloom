services:
  shelfloom:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./.data:/data
      - ./.data/books:/books
      - /path/to/booklore/books:/source:ro
    restart: unless-stopped
